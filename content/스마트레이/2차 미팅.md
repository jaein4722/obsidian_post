---
날짜: Invalid date
---
TODO: [스마트레이](https://www.notion.so/1bbe21d2052480469775e3ef5cc21c78?pvs=21)

[https://snuailab.notion.site/Development-Schedule-4aca73de08db4104b288a3f48be36d6b?pvs=4](https://www.notion.so/4aca73de08db4104b288a3f48be36d6b?pvs=21) 

dialog ui 모듈화 해야됨

작업 방식 dialog 파일 위에 ui 부분 있음. 텍스트 입력, 토글 버튼 등

그거를 복사해서 GPT 한테 ui 파일로 바꿔달라 하면 만들어줌

파일 ui 컨트로 ㄹ 폴더에 만들어서 저장, 그걸 불러오게 만듬.

GPT가 생각보다 파라미터를 까먹음. 일단 그거를 요청하기 전에 직접 feature 실행해보고, 무슨 기능인지 이해, 그 다음 컨픽에 파라미터 다 확인해서 누락되는 파라미터 없게 확인.

수정 순서 있음. 예시 있는데 ui 파일 경로 지정해서 로드, ui 파일에 있는 파라미터들 바인딩. 파라미터별 초기화. 컨픽 파일에 보면 파라미터 정리되어 있음. 이미 지정해놓은 파라미터 값들이 있어 디렉토리 방식으로 저장해서 불러옴. 버튼 및 토글 연결 해주면 됨.

14개 중 몇 개는 탭 구성이 안되어 있음. 디스플레이 등 탭 고정이 안되 있어 ui가 빽빽하게 구성되어 있음. 직접 탭 나눠 작업하면됨.

PyQt 지원하는 탭에서 가져와서 해도 됨.

multi io implementation도 같이 작업. 컨픽에 대해서 잘 살펴봐야 함.

1대1 매칭 말고, 인풋과 아웃풋을 여러개 줄 수 있도록 작업함. 온보드 형식으로 구현하고자 가이드 마련. 모듈 안에 7개 정도 구현되지 않은 함수 있음. 이 기능들을 모듈화시키고 재구현 해야 함. 첫번째로 multi io py파일 제작하고, __init__해서 경로 맞추고 import / 이것들 세팅하는 작업 필요

plane fit 같은 경우는 input output play 3개가 모듈 안에 들어가 있음. 파일들 통해서 내부적으로 불러오는 작업. 인풋에 해당되는 파라미터들, 여기에 나타나야 하는 파라미터들을 정의. 리턴은 딕셔너리 형태로. 잘 모르겠으면 plane_fit 함수 참고.

하위 모듈에서도 init을 설정해야 함. 컨픽 설정할 때 json, yaml 써도 되는데 그거 별로 안좋아해서 py파일로 함. toolbox dict 등 여러 컨픽 있음. 여기에 해당하는 내용 적어주면 됨. 아이콘 등 넣을 예정

tool mapping : 실제 위젯에 기능을 매칭. 아이콘을 눌렀을 때 매칭이 되는 부분. plane fit이라면 planefit tool로 매칭이 될 것.

toolbox parameters 안에 하이퍼파라미터 값들 이런 것들을 정의.

PCA 기반 자동 fitting / dialog 쪽에 구현된 기본 fitting이 있음. 사용자가 직접 파라미터를 입력해서 fitting. 그걸 직접 하지 않고 데이터를 PCA 통해 자동으로 접점 찾아 fitting하게 만드는 기능을 추가할 예정. PCA 기반 fitting을 수행하더라도 parameter를 accept하면 정상적으로 값이 저장되도록 해야 함.

PCA를 통해 새 축을 잡는 느낌. 지금은 plane밖에 없는데, 4가지 feature 대해서 모두 구현해야 함. 일종의 onboarding. 지금까지는 다 좌표계를 이용해서 붙이고 있음. 단순하게 좌표계만 붙여지는게 아니라, 오브젝트와 가장 가깝게 붙을 수 있는 부분이 있어야 함. PCA가 아니더라도 3D 좌표계에 관련된 부분을 온보딩 해보고 감을 잡아보는 곳.

이번 sprint 최종 목표는 핀비전을 정리하는 것. 핀이 어댑터에 물릴 때 잘 물렸는지, 핀이 휘어져있는지 등을 체크하는 게 이번 sprint 목적. 핀 밑바닥에 plane을 깔고, pin top에 대한 z축 값을 빼서 픽셀 값을 주고 threshold 걸어서 불량 여부를 판정해야 함.

기간은 5월 중순까지. 앞선 3개 기능을 이번주에 구현. DM으로 은종님께 정보 받아서 노션에 초대받기. 스케줄 따라 하나의 기능에 대한 티켓 만들어지면 기한 내에 완성하는 것을 목표로.

다음주 수요일에 기능 발표 예정.